## 2026-03-21 08:33
## 2026-03-21
## API工具名称长度限制

1. **工具名称长度限制**：所有通过evolve_self('tool', ...)创建的工具名称必须不超过64个字符。
2. **验证时机**：在工具创建前应进行名称长度验证，避免因超长导致API调用失败。
3. **错误处理**：当工具名称超过64字符时，应自动截断或提示用户修改名称。
4. **命名最佳实践**：建议工具名称保持简洁明了，通常不超过30个字符，为未来扩展留出空间。

这个限制是API层面的硬性约束，违反会导致400错误，因此必须在参数生成阶段就进行预防性验证。